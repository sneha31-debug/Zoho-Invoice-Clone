// Zoho Invoice Clone — Prisma Schema
// Models will be added in upcoming steps.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
